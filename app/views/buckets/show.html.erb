<br>
<%= link_to "Back", buckets_path, class: "Back btn-primary" %>
<br>

<div class = "taskName d-flex justify-content-center">
    <%= @bucket.name %>
</div><br>

<p class ="desLabel"> Description: </p>
<p class = "description"><%= @bucket.description %></p><br><br>

<div class="container text-center">
    <div class="row align-items-center justify-content-center">
        <div class="col-4">
            <p class="showLabel">Pending Tasks</p>
        </div>
        <div class="col-4">
            <p class="showLabel">Completed Tasks</p>
        </div>
    </div>


<div class="row align-self-start justify-content-center">
    <div class="col-4">
        <% @bucket.tasks_by_status("Pending").each do |task| %>
        <p class= "content" style="font-weight:normal"><%= link_to task.name, task_path(task) %></p>
        <% end %>
    </div>

    <div class="col-4">
        <% @bucket.tasks_by_status("Completed").each do |task| %>
        <p class= "content" style="font-weight:normal"><%= link_to task.name, task_path(task) %></p>
        <% end %>
    </div>
<div>
<br><br>



<div class="d-flex align-items-center justify-content-center">
    <div class = "col-2">
        <%= button_to "Edit Bucket", edit_bucket_path(@bucket), class:"button btn-primary", method: :get %>
    </div>
    <div class = "col-2">
        <%= button_to "+ Add Task", new_task_path, class: "button btn-primary", method: :get %></span>
    </div>
</div>
